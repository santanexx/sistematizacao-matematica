[pytest]
testpaths = tests
python_files = test_*.py
addopts = -q --strict-markers

# Coloca a raiz do projeto no sys.path para que `pytest` chamado direto
# encontre o pacote minhastats. Sem isso so funcionava `python -m pytest`,
# que adiciona o diretorio atual por conta propria.
pythonpath = .
